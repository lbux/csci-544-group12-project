Install [uv](https://docs.astral.sh/uv/)

Run `uv sync` to install dependencies

For testing purposes, rename one of the `.jsonl` files to `data.jsonl`

Run `python main.py` to see 2 random visualizations with dummy data